# BIS als App auf iOS installieren

Die BIS-App kann auf iPhone und iPad als eigenständige App auf dem Homebildschirm installiert werden.

## 📱 Installation auf iOS (iPhone/iPad)

### Schritt 1: Safari öffnen
Öffnen Sie die BIS-Webseite in **Safari** (andere Browser unterstützen keine PWA-Installation auf iOS).

```
https://ihre-domain.de
```

### Schritt 2: Teilen-Menü öffnen
1. Tippen Sie auf das **Teilen-Symbol** (Quadrat mit Pfeil nach oben) in der unteren Mitte (iPhone) oder oben rechts (iPad)

### Schritt 3: Zum Home-Bildschirm
2. Scrollen Sie im Menü nach unten
3. Tippen Sie auf **"Zum Home-Bildschirm"**

### Schritt 4: Bestätigen
4. Der Name "BIS" wird automatisch vorgeschlagen
5. Das BIS-Logo (weißer Fisch auf blauem Hintergrund) wird angezeigt
6. Tippen Sie rechts oben auf **"Hinzufügen"**

## ✅ Fertig!

Die BIS-App erscheint nun auf Ihrem Homebildschirm und kann wie eine normale App geöffnet werden:

- **Vollbild-Modus**: Die App läuft ohne Browser-UI
- **Eigenes Icon**: Zeigt das BIS-Logo mit dem weißen Fisch
- **Offline-Fähigkeit**: Grundlegende Funktionen sind auch offline verfügbar
- **Schnellzugriff**: Direkter Zugriff vom Homebildschirm

## 🔧 PWA-Funktionen

Die BIS-App nutzt moderne Web-Technologien:

- **Progressive Web App (PWA)**: Verhält sich wie eine native App
- **Service Worker**: Ermöglicht Offline-Funktionalität
- **Web App Manifest**: Definiert Aussehen und Verhalten
- **Responsive Design**: Optimiert für alle Bildschirmgrößen

## 📋 Systemanforderungen

- **iOS**: Version 12.2 oder höher
- **Browser**: Safari (erforderlich für Installation)
- **Speicherplatz**: Ca. 5-10 MB

## 🎨 Logo

Das BIS-Logo zeigt einen weißen Fisch auf blauem Hintergrund (#0066cc) und wurde speziell für die App-Darstellung optimiert.

## ❓ Probleme?

Falls die Installation nicht funktioniert:

1. **Stellen Sie sicher, dass Sie Safari verwenden** (nicht Chrome oder Firefox)
2. **Prüfen Sie die iOS-Version** (muss mindestens 12.2 sein)
3. **Löschen Sie den Browser-Cache** und versuchen Sie es erneut
4. **Überprüfen Sie die Internetverbindung**

## 🔄 App aktualisieren

Die App aktualisiert sich automatisch, wenn Sie sie öffnen und eine Internetverbindung besteht. Bei größeren Updates kann es notwendig sein, die App vom Homebildschirm zu löschen und neu zu installieren.

## 🗑️ App deinstallieren

Um die App zu entfernen:

1. Halten Sie das BIS-Icon auf dem Homebildschirm gedrückt
2. Tippen Sie auf "App entfernen"
3. Bestätigen Sie mit "Vom Home-Bildschirm entfernen"

---

**Hinweis**: Die Installation als PWA speichert keine Passwörter oder sensiblen Daten lokal. Die Anmeldedaten müssen bei jedem Start neu eingegeben werden, es sei denn, Sie verwenden den Passwort-Manager Ihres Geräts.

