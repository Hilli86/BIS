Flask==3.1.3
Werkzeug==3.1.6
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.3.0
blinker==1.9.0
colorama==0.4.6
reportlab==4.2.2
qrcode==7.4.2
Pillow==12.2.0
docxtpl==0.19.1
docx2pdf==0.1.8
pywebpush==2.0.3
fido2==2.2.0
python-dotenv==1.2.2

# Transitive Abhängigkeit (pywebpush u.a.); Pin fuer pip-audit / CVE-Fixes
cryptography==46.0.7

# Sicherheit
Flask-WTF==1.2.2
flask-limiter==3.12
Flask-Talisman==1.1.0

# Redis-Client fuer flask-limiter in Multi-Worker-Setups (Gunicorn).
# Ohne gesetzte RATELIMIT_STORAGE_URI wird weiterhin memory:// genutzt.
redis==5.0.8

# Datenbank-Abstraktion: SQLAlchemy Core als Fassade, Alembic fuer Schema-
# Migrationen. Wird sowohl gegen SQLite (Default) als auch Postgres genutzt.
SQLAlchemy==2.0.49
alembic==1.18.4

# Postgres-Treiber (Phase 5: Postgres-Readiness). SQLite bleibt der Default;
# psycopg wird erst geladen, wenn DATABASE_URL auf postgresql+psycopg://...
# zeigt. Der [binary]-Extra bringt fertige C-Wheels mit, damit kein libpq /
# pg_config auf dem Zielsystem installiert sein muss.
psycopg[binary]==3.2.3

# Tests (CI / lokale Qualitätssicherung)
pytest==9.0.3
